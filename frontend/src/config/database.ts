const DATABASE_NAME = '@gamecenter';

// @app:collection

const COLLECTION_USERS = `${DATABASE_NAME}:user`
const COLLECTION_GAMES = `${DATABASE_NAME}:games`

export {
COLLECTION_USERS,
COLLECTION_GAMES
}